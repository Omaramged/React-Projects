import React from 'react';

function CTA() {
  return (
    <section className="cta-section">
      <div className="cta-box">
        <h2 className="cta-title">Join now and get 30% OFF!</h2>
        <p className="cta-subtitle">Grab your ticket now before the discount ends.</p>
        <button className="cta-btn">GET TICKET</button>
      </div>
    </section>
  );
}

export default CTA;
